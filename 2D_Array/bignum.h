#ifndef _BIGNUM_H
#define _BIGNUM_H

#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

char convert_character(int n);
int convert_digit(char c);
int _strcpy(char *b, char *src);
int _strlen(char *s);

#endif
